1. 8888 앱의 `app.py` 내부 `_load_exits_modes()` 함수 개선:
   - 과거 `trade_history.csv`의 청산 로그에 '역방향'으로 잘못 기록된 데이터(프로세스 재시작 시 메모리 증발로 인한 fallback 오류)가 있더라도, '진입' 시 기록된 정확한 매매모드(순방향/역방향)를 먼저 파악하여 덮어쓰도록 로직을 수정합니다.
2. 8402 앱의 메모리 유실 방지:
   - 향후 8402 봇이 재시작되더라도 청산 시 잘못된 매매모드가 기록되지 않도록, `csv_log` 저장 시 과거 진입 로그를 역추적하는 안전장치(`get_last_entry_mode`)를 추가하거나 `trade_mode` 폴백 로직을 강화합니다.
